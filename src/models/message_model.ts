export interface InterfaceMessages {
    // да, это не модель
    // да, сейчас это строки
    id: String;
    messageText: String;
    userIdFrom: String;
    conversationIdTo: String;
    createdAt: String;
    typeOfMessage: String;
}

